apiVersion: apps/v1
kind: Deployment
metadata:
  name: driver
spec:
  selector:
    matchLabels:
      app: driver
  template:
    metadata:
      labels:
        app: driver
    spec:
      containers:
        - name: driver
          image: img-deployment
          ports:
            - containerPort: 8081

---
apiVersion: v1
kind: Service
metadata:
  name: driver-service
spec:
  ports:
    - port: 8081
      targetPort: 8081
  selector:
    app: driver

---
apiVersion: v1
kind: Gateway
metadata:
  name: driver-gateway
spec:
  servers:
    - port:
        number: 80
        name: http
        protocol: HTTP
      hosts:
        - "*"
  selector:
    istio: ingressgateway
